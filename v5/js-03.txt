':''}" data-tab="theater"><span>▶️</span>المسرح</button><button class="nav ${tab==='lessons'?'on':''}" data-tab="lessons"><span>📚</span>الدروس</button><button class="nav ${tab==='family'?'on':''}" data-tab="family"><span>👨‍👩‍👧</span>معًا</button></nav>`}
function wireShell(){document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>navigate(b.dataset.tab));const s=$('#sound');if(s)s.onclick=()=>{state.sound=!state.sound;save();renderHome();};const e=$('#explore');if(e)e.onclick=renderExplore}
function gameCard(g,wide=false){return `<button class="card ${wide?'wideCard':''}" data-game="${g.id}"><span class="badge">${starsFor(g.id)?'⭐'.repeat(starsFor(g.id)):'جديد'}</span><span class="hat">🧢👓</span><span class="icon">${g.icon}</span><strong>${g.title}</strong><small>${g.sub}</small></button>`}
function wireCards(){document.querySelectorAll('[data-game]').forEach(b=>b.onclick=()=>startGame(b.dataset.game));document.querySelectorAll('[data-cat]').forEach(b=>b.onclick=()=>renderExplore(b.dataset.cat))}
function renderHome(){
 const daily=G.slice((new Date().getDate()*3)%G.length).concat(G).slice(0,7);const recent=state.recent.map(id=>G.find(g=>g.id===id)).filter(Boolean);const most=[...G].sort((a,b)=>starsFor(b.id)-starsFor(a.id)).slice(0,7);
 app.innerHTML=`<div class="app">${topbar()}<section class="hero"><div><div class="pill">⭐ ${state.stars}</div><h1>ماذا نلعب اليوم؟</h1><p>ألعاب قصيرة وسهلة. اختر وابدأ.</p><div class="heroBtns"><button class="big" id="surprise">🎲 لعبة مفاجأة</button><button class="big alt" id="dailyGift">🎁 هدية اليوم</button></div></div><div class="mascot">🧢</div></section>
 ${rowSection('اختيارات اليوم','✨',daily)}
 ${recent.length?rowSection('لعبت مؤخرًا','🕘',recent):''}
 ${rowSection('الأكثر لعبًا','🔥',most)}
 <section class="section"><div class="sectionTitle"><h2>عوالم نستكشفها</h2><span class="pill">${Object.keys(cats).length} مواضيع</span></div><div class="topicGrid">${Object.entries(cats).map(([id,[name,ic]])=>`<button class="topic" data-cat="${id}"><span>${ic}</span>${name}</button>`).join('')}</div></section>
 ${rowSection('جديد','🆕',G.slice(-10))}${bottom('play')}</div>`;
 wireShell();wireCards();$('#surprise').onclick=()=>startGame(G[Math.floor(Math.random()*G.length)].id);$('#dailyGift').onclick=dailyGift;
}
function rowSection(title,icon,arr){return `<section class="section"><div class="sectionTitle"><h2>${icon} ${title}</h2><span class="pill">${arr.length}</span></div><div class="row">${arr.map(g=>gameCard(g)).join('')}</div></section>`}
function dailyGift(){const today=new Date().toDateString();if(state.daily===today)return toast('فتحت هدية اليوم 🎁');state.daily=today;state.stars+=5;save();modal('🎁','هدية اليوم','حصلت على ٥ نجوم!','رائع!');success()}
function renderExplore(cat){const list=cat?G.filter(g=>g.cat===cat):G;app.innerHTML=`<div class="app">${topbar()}<main class="page"><div class="pageHead"><button class="back" id="back">→</button><h1>${cat?cats[cat][1]+' '+cats[cat][0]:'استكشف كل الألعاب'}</h1></div><div class="catalog">${list.map(g=>gameCard(g)).join('')}</div></main>${bottom('play')}</div>`;wireShell();wireCards();$('#back').onclick=renderHome}
function renderTheater(){app.innerHTML=`<div class="app">${topbar()}<main class="page"><div class="pageHead"><h1>▶️ المسرح</h1></div>${shows.map((s,i)=>`<button class="showCard" data-show="${i}" style="width:100%;border:0;text-align:right"><div class="showIcon">${s[0]}</div><div><h3>${s[1]}</h3><p>${s[2]}</p></div></button>`).join('')}</main>${bottom('theater')}</div>`;wireShell();document.querySelectorAll('[data-show]').forEach(b=>b.onclick=()=>playShow(+b.dataset.show))}
function playShow(i){const s=shows[i];modal(s[0],s[1],s[2]+'\n\nاضغط واستمع، ثم ابحث عن الفكرة حولك.','انتهى');speak(s[1]+'. '+s[2])}
function renderLessons(){app.innerHTML=`<div class="app">${topbar()}<main class="page"><div class="pageHead"><h1>📚 الدروس</h1></div>${lessons.map(l=>{const d=l.games.filter(id=>starsFor(id)>0).length;return `<div class="lessonCard"><h3>${l.icon} ${l.title}</h3><p>${l.desc}</p><div class="lessonSteps">${l.games.map((_,i)=>`<span class="dot ${i<d?'done':''}"></span>`).join('')}</div><button class="big" style="margin-top:12px" data-lesson="${l.id}">ابدأ</button></div>`}).join('')}</main>${bottom('lessons')}</div>`;wireShell();document.querySelectorAll('[data-lesson]').forEach(b=>b.onclick=()=>startLesson(b.dataset.lesson))}
function startLesson(id){const l=lessons.find(x=>x.id===id);const next=l.games.find(g=>!starsFor(g))||l.games[0];startGame(next)}
function renderFamily(){app.innerHTML=`<div class="app">${topbar()}<main class="page"><div class="pageHead"><h1>👨‍👩‍👧 نلعب معًا</h1></div>${family.map((f,i)=>`<button class="familyCard" data-family="${i}" style="width:100%;border:0;text-align:right"><h3>${f[0]} ${f[1]}</h3><p>${f[2]}</p></button>`).join('')}</main>${bottom('family')}</div>`;wireShell();document.querySelectorAll('[data-family]').forEach(b=>b.onclick=()=>{const f=family[+b.dataset.family];modal(f[0],f[1],f[2],'جاهزون!');speak(f[1]+'. '+f[2])})}
function navigate(tab){({play:renderHome,theater:renderTheater,lessons:renderLessons,family:renderFamily}[tab]||renderHome)()}
function startGame(id){const g=G.find(x=>x.id===id);if(!g)return;state.recent=[id,...state.recent.filter(x=>x!==id)].slice(0,8);save();({quiz:quizGame,tap:tapGame,memory:memoryGame,sort:sortGame,sequence:sequenceGame,hidden:hiddenGame,build:buildGame,robot:robotGame,coding:codingGame,music:musicGame,rhythm:rhythmGame,paint:paintGa