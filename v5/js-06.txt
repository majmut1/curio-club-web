.dataset.s);$('#stage').onclick=e=>{const r=e.currentTarget.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top,s=document.createElement('span');s.textContent=sel;s.style.cssText=`position:absolute;font-size:48px;left:${x-24}px;top:${y-24}px`;e.currentTarget.appendChild(s);beep(620)};$('#done').onclick=()=>{award(g.id,3);modal('✨','جميل جدًا!','زينت عالمك.','مرة ثانية',()=>stickerGame(g))}}
function traceGame(g){gameShell(g,`<div class="guide">ارسم فوق حرف أ</div><canvas class="canvas" id="canvas"></canvas><button class="big" id="done" style="display:block;margin:12px auto">انتهيت</button>`,'✍️');const c=$('#canvas'),ctx=c.getContext('2d');c.width=c.clientWidth*2;c.height=c.clientHeight*2;ctx.scale(2,2);ctx.font='bold 240px system-ui';ctx.textAlign='center';ctx.textBaseline='middle';ctx.strokeStyle='#cdd8ec';ctx.lineWidth=7;ctx.setLineDash([12,10]);ctx.strokeText('أ',c.clientWidth/2,c.clientHeight/2);ctx.setLineDash([]);ctx.lineWidth=15;ctx.lineCap='round';ctx.strokeStyle='#ff8a24';let down=false,last;const pos=e=>{const r=c.getBoundingClientRect();return[e.clientX-r.left,e.clientY-r.top]};c.onpointerdown=e=>{down=true;last=pos(e)};c.onpointermove=e=>{if(!down)return;const p=pos(e);ctx.beginPath();ctx.moveTo(...last);ctx.lineTo(...p);ctx.stroke();last=p};c.onpointerup=()=>down=false;$('#done').onclick=()=>{award(g.id,3);modal('أ','أحسنت!','تتبعت الحرف.','مرة ثانية',()=>traceGame(g))};speak('ارسم فوق حرف ألف')}
function movementGame(g){let i=0;gameShell(g,`<div class="guide">تحرك معي</div><div class="prompt"><div class="emoji" id="mvIcon">${g.moves[0][0]}</div><h2 id="mvText">${g.moves[0][1]}</h2></div><button class="big" id="next" style="display:block;margin:18px auto">التالي</button>`,'1 / 3');speak(g.moves[0][1]);$('#next').onclick=()=>{i++;if(i>=g.moves.length){award(g.id,3);return modal('⭐','حركة رائعة!','أنهيت الحركات.','مرة ثانية',()=>movementGame(g))}$('#mvIcon').textContent=g.moves[i][0];$('#mvText').textContent=g.moves[i][1];$('#score').textContent=`${i+1} / 3`;speak(g.moves[i][1])}}
function modal(icon,title,text,button='حسنًا',cb){const w=document.createElement('div');w.className='modalWrap';w.innerHTML=`<div class="modal"><div class="heroEmoji">${icon}</div><h2>${title}</h2><p style="font-size:19px;font-weight:800;white-space:pre-line">${text}</p><button class="big" id="modalBtn">${button}</button><button class="big alt" id="homeBtn" style="margin-right:8px">الرئيسية</button></div>`;document.body.appendChild(w);w.querySelector('#modalBtn').onclick=()=>{w.remove();cb&&cb()};w.querySelector('#homeBtn').onclick=()=>{w.remove();renderHome()}}
if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js').catch(()=>{}));
renderHome();
