me,stickers:stickerGame,trace:traceGame,movement:movementGame}[g.mode]||quizGame)(g)}
function gameShell(g,body,score='0 / 3'){app.innerHTML=`<main class="game"><div class="gameTop"><button class="back" id="gback">→</button><div class="gameTitle"><h1>${g.icon} ${g.title}</h1><p>${g.sub}</p></div><div class="score" id="score">${score}</div></div><section class="play">${body}</section></main>`;$('#gback').onclick=renderHome}
function quizGame(g){let i=0,score=0;gameShell(g,`<div id="quiz"></div><div class="msg" id="msg"></div>`);const box=$('#quiz'),sc=$('#score'),msg=$('#msg');function round(){if(i>=g.rounds.length){award(g.id,score===g.rounds.length?3:2);return modal('⭐','أحسنت!','أنهيت اللعبة.','العب مرة أخرى',()=>quizGame(g))}const r=g.rounds[i];box.innerHTML=`<div class="guide">${r.text}</div><div class="prompt"><div class="emoji">${r.emoji}</div></div><div class="answers">${r.answers.map((a,n)=>`<button class="ans" data-a="${n}">${a}</button>`).join('')}</div>`;speak(r.text);box.querySelectorAll('.ans').forEach(b=>b.onclick=()=>{const n=+b.dataset.a;if(n===r.correct){b.classList.add('good');score++;msg.textContent='أحسنت! ⭐';success()}else{b.classList.add('bad');msg.textContent='قريب! جرّب التالي';wrong()}i++;sc.textContent=`${i} / ${g.rounds.length}`;setTimeout(()=>{msg.textContent='';round()},650)})}round()}
function tapGame(g){let n=0;gameShell(g,`<div class="guide">المس ${g.target}</div><div class="tapStage ${g.bg||''}" id="stage"></div><div class="msg" id="msg">ابدأ!</div>`,`0 / ${g.count}`);const st=$('#stage'),sc=$('#score');if(g.bg==='wash')st.innerHTML='<div style="font-size:120px;position:absolute;left:50%;top:50%;transform:translate(-50%,-50%)">🚗</div>';function add(){for(let k=0;k<Math.min(4,g.count-n);k++){const b=document.createElement('button');b.className='target';b.textContent=g.target;b.style.left=(8+Math.random()*78)+'%';b.style.top=(8+Math.random()*68)+'%';b.onclick=()=>{b.remove();n++;sc.textContent=`${n} / ${g.count}`;beep(700);if(n>=g.count){award(g.id,3);modal('⭐','رائع!','أنهيت اللعبة.','مرة ثانية',()=>tapGame(g))}else add()};st.appendChild(b)}}add();speak('المس '+g.title)}
function memoryGame(g){const vals=['🐶','🐱','🚗','⭐'];const deck=[...vals,...vals].sort(()=>Math.random()-.5);let open=[],done=0;gameShell(g,`<div class="guide">افتح بطاقتين متشابهتين</div><div class="memory">${deck.map((_,i)=>`<button class="mem" data-i="${i}">?</button>`).join('')}</div><div class="msg" id="msg"></div>`,'0 / 4');const cards=[...document.querySelectorAll('.mem')];cards.forEach(c=>c.onclick=()=>{const i=+c.dataset.i;if(open.includes(i)||c.classList.contains('done')||open.length===2)return;c.textContent=deck[i];c.classList.add('open');open.push(i);if(open.length===2){const[a,b]=open;if(deck[a]===deck[b]){cards[a].classList.add('done');cards[b].classList.add('done');done++;$('#score').textContent=`${done} / 4`;open=[];success();if(done===4){award(g.id,3);setTimeout(()=>modal('🧠','ذاكرة ممتازة!','وجدت كل الأزواج.','مرة ثانية',()=>memoryGame(g)),400)}}else{wrong();setTimeout(()=>{[a,b].forEach(x=>{cards[x].textContent='?';cards[x].classList.remove('open')});open=[]},600)}}})}
function sortGame(g){let idx=0,ok=0;const items=[...g.items].sort(()=>Math.random()-.5).slice(0,Math.min(6,g.items.length));gameShell(g,`<div class="guide">اختر المكان الصحيح</div><div class="sortItem" id="item"></div><div class="sortBins" id="bins"></div><div class="msg" id="msg"></div>`,`0 / ${Math.min(3,items.length)}`);function next(){if(idx>=Math.min(3,items.length)){award(g.id,ok===3?3:2);return modal('⭐','أحسنت!','انتهى الفرز.','مرة ثانية',()=>sortGame(g))}const item=items[idx];$('#item').textContent=item[0];$('#bins').innerHTML=g.bins.map(x=>`<button class="bin" data-v="${x}">${x}</button>`).join('');speak('اختر المكان الصحيح');document.querySelectorAll('.bin').forEach(b=>b.onclick=()=>{if(b.dataset.v===item[1]){ok++;success();$('#msg').textContent='صحيح!'}else{wrong();$('#msg').textContent='جرّب التالي'}idx++;$('#score').textContent=`${idx} / 3`;setTimeout(()=>{$('#msg').textContent='';next()},600)})}next()}
function sequenceGame(g){let step=0;gameShell(g,`<div class="guide">اضغط الخطوات بالترتيب</div><div class="seq">${g.steps.map((s,i)=>`<button class="seqBtn" data-i="${i}">${s}</button>`).join('')}</div><div class="msg" id="msg"></div>`,`0 / ${g.steps.length}`);document.querySelectorAll('.seqBtn').forEach(b=>b.onclick=()=>{if(+b.dataset.i===step){b.classList.add('done');b.disabled=true;step++;$('#score').textContent=`${step} / ${g.steps.length}`;success();if(step===g.steps.length){award(g.id,3);setTimeout(()=>modal('🧼','ممتاز!','رتبت الخطوات صح.','مرة ثانية',()=>sequenceGame(g)),400)}}else{wrong();$('#msg').textContent='ابدأ من الخطوة التالية الصحيحة';setTimeout(()=>$('#msg').textContent='',700)}});speak('اضغط الخطوات بالترتيب')}
function hiddenGame(g){let found=0;const dec=['🍎','🥄','🧸','🌼','⚽','🧢','📘','🧃'];gameShell(g,`<div class="guide">ابحث عن: ${g.targets.join(' ')}</div><div class="hiddenStage" id="stage"></div><div class="msg">المس الأشياء المطلوبة</div>`,`0 / ${g.targets.length}`);const st=$('#stage');[...g.targets,...dec].forEach((x,i)=>{const b=document.createElement('button');b.className='target';b.textContent=x;b.style.fontSize=(36+Math.random()*28)+'px';b.style.left=(5+Math.random()*82)+'%';b.style.top=(5+Math.random()*72)+'%';b.onclick=()=>{if(g.targets.includes(x)){b.remove();found++;$('#score').textContent=`${found} / ${g.targets.length}`;success();if(found===g.targets.length){award(g.id,3)